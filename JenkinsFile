pipeline {
  agent any
  stages {

    stage('download dependencies') {
      steps {
        sh 'npm ci'
        sh 'npm outdated || exit 0'
      }
    }
    
    stage('Build') {
      steps {
        sh 'npm run build'
      }
    }
  }
}